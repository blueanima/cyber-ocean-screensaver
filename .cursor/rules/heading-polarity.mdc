---
description: 新生物如何确定头尾与中线
alwaysApply: true
---

# 头尾标定

加物种时必须在 `formulas.rs` 的 `Species.heading` 选一类，并出对照图（`python3 scripts/heading-audit/heads_tails.py`）确认。

中线是身体脊椎（左右对称轴），头尾只在中线两端，不在翅膀、触须、附肢边上。禁止用包围盒最长弦硬拉一条轴。

| HeadingKind | 何时用 | 中线 | 头 |
|---|---|---|---|
| `Radial` | n 次旋转拷贝 | 无 | 无头 |
| `Bell` | 伞盖/口端水母 | `k=0` 身体曲线 | 伞盖 = 中线最下方 |
| `SpineNoLegs` | 附肢在 k≈0 炸开 | 去掉附肢后的身体曲线 | 朝航向的一端 |
| `Torso` | 有翅膀 | 躯干中线，不含翅膀 | 身体尖端，不在翅尖 |
| `Spine` | 其余细长身体 | 沿身体参数取每节中点 | 朝航向的一端 |

`SPECIES_FACE` 必须让头锁航向（头→尾向量转到 +X）。花、轮、星无头。改头尾后要重算 face，并升补丁号。
